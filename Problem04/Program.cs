using System;
using System.Linq;
using System.Collections.Generic;
using ACMHelper;

namespace Problem04
{
    /*
     * Write a function that given a list of non negative integers, arranges them such 
     * that they form the largest possible number. For example, given [50, 2, 1, 9], 
     * the largest formed number is 95021.
     * 
     * Demonstrate your function works by making additional tests of your own.
     */
    class Program
    {
        public static int LargestNumber (List<int> nums)
        {
            return 0;
        }

        static void Main(string[] args)
        {
            List<int> input = "50 2 1 9".ToIntegerList();

            Console.WriteLine(input);
        }
    }
}
